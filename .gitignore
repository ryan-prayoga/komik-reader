# Suwayomi persistent data
suwayomi/data/

# Dependencies
node_modules/

# Build
web/.svelte-kit/
web/build/
web/dist/

# Env & secrets (never commit)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
secrets/

# Auth / local data
**/auth.db
**/auth.db-*
web/data/

# OS
.DS_Store

# Logs
*.log

# Playwright MCP artifacts
.playwright-mcp/

.claude/
.omo/
# No trailing slash: this is a symlink to an out-of-repo agent config, and git
# treats a symlink as a file, so a dir-only pattern would never match it.
.opencode

